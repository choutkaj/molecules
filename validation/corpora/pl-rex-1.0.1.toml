id = "pl-rex-1.0.1"
title = "PL-REX 1.0.1 protein-ligand complexes"
local_path = "validation/data/PL-REX-1.0.1"
source_kind = "user-provided-local-corpus"
primary_formats = ["sdf-v2000", "pdb"]
scope = ["io.sdf.v2000.parse", "algo.rings.fast", "algo.aromaticity.rdkit-like"]
future_scope = ["io.pdb.parse", "bio.hierarchy.smcra"]
ci_tier = "optional-local-corpus"

notes = [
  "Ligand SDF files are useful real-world parser and small-molecule algorithm inputs.",
  "Protein and receptor files are PDB, not mmCIF, so they should not be used as direct io.mmcif.parse validation.",
  "Several ligand files include charge records such as M  CHG; failures against current prototype behavior should be tracked rather than hidden.",
  "Do not commit the raw corpus files to git.",
]
