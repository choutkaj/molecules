id = "query.smarts"
title = "SMARTS query parsing"
area = "query"
version = 2
implemented = true
validated = true
description = "Parse a deliberately bounded, resource-limited SMARTS subset into the syntax-independent query graph."
depends_on = ["query.graph"]
validation_required = ["smoke"]
