id = "stereo.perception"
title = "Stereochemistry perception"
area = "stereo"
version = 2
implemented = true
validated = true
description = "Validate local stereo elements, detect candidate stereo units, and assemble paired directional source marks."
depends_on = ["stereo.representation", "chem.sanitize.rdkit-like", "algo.canonical-ranking"]
validation_required = ["smoke"]
