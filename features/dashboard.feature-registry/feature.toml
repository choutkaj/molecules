id = "dashboard.feature-registry"
title = "Feature registry and dashboard"
area = "infrastructure"
domains = ["infrastructure"]
version = 13
status = "supported"
description = "Keep release-status and dependency metadata as the source of truth, validate the feature graph, and generate the dashboard from it."
depends_on = []
validation_required = []
