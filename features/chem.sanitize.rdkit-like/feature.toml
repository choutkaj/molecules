id = "chem.sanitize.rdkit-like"
title = "RDKit-like sanitization pipeline"
area = "chemistry"
version = 14
implemented = true
description = "Run explicit valence, ring, aromaticity, and stereo perception as an opt-in small-molecule sanitization pipeline."
depends_on = ["algo.valence.rdkit-like", "algo.rings.sssr", "algo.aromaticity.rdkit-like", "stereo.perception"]
validation_required = ["smoke"]
