id = "chem.sanitize.rdkit-like"
title = "RDKit-like sanitization pipeline"
area = "chemistry"
version = 1
implemented = true
validated = false
description = "Run explicit valence, ring, and aromaticity perception as an opt-in small-molecule sanitization pipeline."
depends_on = ["algo.valence.rdkit-like", "algo.rings.sssr", "algo.aromaticity.rdkit-like"]
