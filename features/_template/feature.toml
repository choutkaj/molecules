id = "area.name"
title = "Human-readable feature title"
area = "core"
version = 1
implemented = false
validated = false
description = "One-sentence description."

# depends_on = ["core.graph"]
# reference = ["rdkit.Chem.example"]
# public_api = ["molecules::path::function"]
