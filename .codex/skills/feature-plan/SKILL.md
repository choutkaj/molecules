# Feature implementation plan

Start by reading `ARCHITECTURE.md`, `AGENTS.md`, and the requested `features/<feature-id>/` directory. Keep work scoped to the requested feature.

## Task

Given a feature ID, write a concrete implementation plan.

## Output

Write or update `features/<feature-id>/implementation_plan.md` with public API, internal modules touched, data model changes, algorithm outline, tests, reference validation format, risks, and edge cases.

Do not implement code in this skill.
